services:
  - type: web
    name: portfolio-backend
    env: go
    buildCommand: go build -o main main.go
    startCommand: ./main
    envVars:
      - key: PORT
        value: 8080
    healthCheckPath: /health
